import Layout from "../Layout/Layout";
import {Button, Center} from "@chakra-ui/react";

export const Mint = () => {
    return <Layout>
        <Center>
            <Button>Mint</Button>
        </Center>
    </Layout>
}