import prisma from "../prisma";
import {User} from "@prisma/client";
import {getRandomSmallLetter, getRandomStringWithSmallLetters} from "../modules/random";
import {UserService} from "./userService";

export class AuthService {
    private static generateAuthMessageText (wallet_address: string){

    }

    public static getAuthMessage (wallet_address: string){

    }

    public static handleSignIn(wallet_address: string){

    }
}